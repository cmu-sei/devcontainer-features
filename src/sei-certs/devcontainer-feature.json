{
    "id": "sei-certs",
    "version": "1.0.0",
    "name": "SEI Certificates",
    "description": "Installs the SEI root CAs and the Zscaler root CA into the system trust store, and points Node, Python requests, pip and OpenSSL at it",
    "containerEnv": {
        "NODE_EXTRA_CA_CERTS": "/etc/ssl/certs/ca-certificates.crt",
        "REQUESTS_CA_BUNDLE": "/etc/ssl/certs/ca-certificates.crt",
        "PIP_CERT": "/etc/ssl/certs/ca-certificates.crt",
        "SSL_CERT_FILE": "/etc/ssl/certs/ca-certificates.crt"
    }
}
