{
    "id": "pi",
    "version": "1.0.0",
    "name": "Pi Coding Agent",
    "description": "Installs the Pi Coding Agent CLI via npm",
    "postCreateCommand": "bash -l /usr/local/share/org-features/pi/postcreate.sh",
    "dependsOn": {
        "ghcr.io/devcontainers/features/node:2": {}
    },
    "options": {
        "version": {
            "type": "string",
            "default": "0.87.0",
            "description": "Exact upstream tool version installed at image build time."
        }
    }
}
